## 2026-02-05 Tasks

Prior Agent Conversation:
We have set up a scraping infrastructure for 'events-the-list-but-others'.
Current State:

1. Codebase:
   - [scripts/scrape.ts](cci:7://file:///Users/robingamedev/dev/websites/events-the-list-but-others/scripts/scrape.ts:0:0-0:0): Runs the pipeline.
   - `src/adapters/`: Contains `HTMLScraperAdapter` (working) and `JSONAdapter`.
   - `data/`: Stores results.
     - `data/cache/`: Local HTML cache (gitignored).
     - `data/scraped/`: JSON snapshots (committed).
     - `_data/`: Final production data (committed).
2. Naming Convention: Results are saved as `YYYY-MM-DD-{source}.json`.
3. Progress:
   - "Kiln" scraper is fully operational (HTML scraping).
   - "Staging Storage" is complete.

Next Task:
We need to implement the `RSSAdapter` to handle standard feed sources (e.g. from `docs/project/pdx-sites.md`).
Please check `task.md` and `implementation_plan.md` for details.

Also `docs/project/project-doc-v2.md` for more details.

### Goals:

1. Get the RSSAdapter working
2. See if meetup.com has a RSS feed
3. If not, figure out how to get https://www.meetup.com/find/?location=us--or--portland&eventType=inPerson&source=EVENTS&categoryId=546
